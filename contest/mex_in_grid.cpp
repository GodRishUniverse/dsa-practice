#include <vector>
#include <string>
#include <algorithm>
#include <iostream>
#define ll long long

using namespace std;
int main(){
    int tests;
    
    cin >> tests;


    for (int i =0; i <tests; i++){
        int n;
        cin >> n;

        // base case n = 1 - nothing should be printed ad there is no minimum value excluded
        if (n ==1){
            cout << "" <<endl;
        }
        // 0 should be closer to smaller values and very large values
        // 0 should be in the middle
        // smaller values are in the middle

        // spiral values are the 
        //TODO: implement
    }
    
}
